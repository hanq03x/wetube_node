extends ../base

block content
    if errorMessage
        span=errorMessage
    form(method="POST")
        input(type="password", name="oldPswd", placeholder="현재 비밀번호")
        input(type="password", name="newPswd", placeholder="새로운 비밀번호")
        input(type="password", name="confirm", placeholder="새로운 비밀번호 확인")
        input(type="submit", value="비밀번호 변경")